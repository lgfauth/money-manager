[build]
builder = "DOCKERFILE"
dockerfilePath = "src/Frontends/MoneyManager.Web/Dockerfile"
watchPatterns = ["src/Frontends/MoneyManager.Web/**"]

[build.args]
NEXT_PUBLIC_API_URL = "${{API_URL}}"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
