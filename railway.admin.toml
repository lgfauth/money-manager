[build]
builder = "DOCKERFILE"
dockerfilePath = "src/Frontends/MoneyManager.Backoffice/Dockerfile"
watchPatterns = ["src/Frontends/MoneyManager.Backoffice/**"]

[build.args]
NEXT_PUBLIC_ADMIN_API_URL = "${{ADMIN_API_URL}}"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
