@using Microsoft.AspNetCore.Components.Authorization
@using Microsoft.AspNetCore.Components.Routing
@using Microsoft.AspNetCore.Components.Web
@using MoneyManager.Web.Shared

<CascadingAuthenticationState>
    <meta charset="utf-8" />
    <Router AppAssembly="@typeof(App).Assembly">
        <Found Context="routeData">
            <AuthorizeRouteView RouteData="@routeData" DefaultLayout="@typeof(MainLayout)">
                <NotAuthorized>
                    <RedirectToLogin />
                </NotAuthorized>
                <Authorizing>
                    <BusyOverlay IsBusy="true" Message="Autenticando..." />
                </Authorizing>
            </AuthorizeRouteView>
            <FocusOnNavigate RouteData="@routeData" Selector="h1" />
        </Found>
        <NotFound>
            <PageTitle>PÃ¡gina nÃ£o encontrada</PageTitle>
            <LayoutView Layout="@typeof(MainLayout)">
                <p role="alert">Desculpe, nÃ£o conseguimos encontrar a pÃ¡gina que vocÃª estava procurando.</p>
            </LayoutView>
        </NotFound>
    </Router>
</CascadingAuthenticationState>
