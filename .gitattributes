# Ensure shell scripts always use LF line endings
*.sh text eol=lf
docker-entrypoint.sh text eol=lf

# Ensure Dockerfiles use LF
Dockerfile* text eol=lf

# Auto detect text files and normalize to LF
* text=auto eol=lf

# Force UTF-8 for UI/resource files to prevent mojibake with accented characters
*.razor text working-tree-encoding=UTF-8 eol=lf
*.cshtml text working-tree-encoding=UTF-8 eol=lf
*.html text working-tree-encoding=UTF-8 eol=lf
*.css text working-tree-encoding=UTF-8 eol=lf
*.js text working-tree-encoding=UTF-8 eol=lf
*.json text working-tree-encoding=UTF-8 eol=lf
